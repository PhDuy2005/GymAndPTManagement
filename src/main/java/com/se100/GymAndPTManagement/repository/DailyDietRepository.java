package com.se100.GymAndPTManagement.repository;

import java.time.LocalDate;
import java.util.List;
import java.util.Optional;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

import com.se100.GymAndPTManagement.domain.table.DailyDiet;

/**
 * Generated by: GitHub Copilot (Claude Sonnet 4.5)
 * Created by: PhDuy2005
 * Created at: 2026-01-15 16:00:00
 * Purpose: Repository for DailyDiet entity with JPA method naming
 */
@Repository
public interface DailyDietRepository extends JpaRepository<DailyDiet, Long>, JpaSpecificationExecutor<DailyDiet> {

    // Find by member
    List<DailyDiet> findByMemberId(Long memberId);

    Page<DailyDiet> findByMemberId(Long memberId, Pageable pageable);

    // Find by member and date
    Optional<DailyDiet> findByMemberIdAndDietDate(Long memberId, LocalDate dietDate);

    // Find by PT
    List<DailyDiet> findByPersonalTrainerId(Long ptId);

    Page<DailyDiet> findByPersonalTrainerId(Long ptId, Pageable pageable);

    // Find by date range
    List<DailyDiet> findByDietDateBetween(LocalDate startDate, LocalDate endDate);

    Page<DailyDiet> findByDietDateBetween(LocalDate startDate, LocalDate endDate, Pageable pageable);

    // Find by member and date range
    List<DailyDiet> findByMemberIdAndDietDateBetween(Long memberId, LocalDate startDate, LocalDate endDate);

    Page<DailyDiet> findByMemberIdAndDietDateBetween(Long memberId, LocalDate startDate, LocalDate endDate,
            Pageable pageable);

    // Check if diet exists for member on specific date
    boolean existsByMemberIdAndDietDate(Long memberId, LocalDate dietDate);
}
