package com.se100.GymAndPTManagement.domain.responseDTO;

import java.math.BigDecimal;
import java.time.Instant;

import com.se100.GymAndPTManagement.domain.table.AdditionalService;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * Generated by: GitHub Copilot (Claude Sonnet 4.5)
 * Created by: PhDuy2005
 * Created at: 2026-01-11 19:51:58
 * Purpose: Response DTO for additional service
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class ResAdditionalServiceDTO {
    private Long id;
    private String name;
    private BigDecimal costPrice;
    private BigDecimal suggestSellPrice;
    private String description;
    private Boolean isActive;

    private Instant createdAt;
    private Instant updatedAt;
    private String createdBy;
    private String updatedBy;

    public static ResAdditionalServiceDTO fromEntity(AdditionalService additionalService) {
        return ResAdditionalServiceDTO.builder()
                .id(additionalService.getId())
                .name(additionalService.getName())
                .costPrice(additionalService.getCostPrice())
                .suggestSellPrice(additionalService.getSuggestSellPrice())
                .description(additionalService.getDescription())
                .isActive(additionalService.getIsActive())
                .createdAt(additionalService.getCreatedAt())
                .updatedAt(additionalService.getUpdatedAt())
                .createdBy(additionalService.getCreatedBy())
                .updatedBy(additionalService.getUpdatedBy())
                .build();
    }
}
