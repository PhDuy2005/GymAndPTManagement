package com.se100.GymAndPTManagement.domain.requestDTO;

import java.math.BigDecimal;
import java.time.LocalDate;

import jakarta.validation.constraints.DecimalMin;
import jakarta.validation.constraints.NotNull;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

/**
 * Generated by: GitHub Copilot (Claude Sonnet 4.5)
 * Created by: PhDuy2005
 * Created at: 2026-01-14 12:00:00
 * Purpose: Request DTO for updating body metrics
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class ReqUpdateBodyMetricsDTO {

    @NotNull(message = "Measured date is required")
    private LocalDate measuredDate;

    @DecimalMin(value = "0.0", inclusive = false, message = "Weight must be greater than 0")
    private BigDecimal weight;

    @DecimalMin(value = "0.0", inclusive = false, message = "Height must be greater than 0")
    private BigDecimal height;

    @DecimalMin(value = "0.0", inclusive = false, message = "Muscle mass must be greater than 0")
    private BigDecimal muscleMass;

    @DecimalMin(value = "0.0", message = "Body fat percentage must be greater than or equal to 0")
    private BigDecimal bodyFatPercentage;
}
