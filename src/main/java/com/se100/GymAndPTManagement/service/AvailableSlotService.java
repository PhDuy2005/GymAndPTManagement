package com.se100.GymAndPTManagement.service;

import java.util.List;
import java.util.stream.Collectors;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.domain.Specification;
import org.springframework.stereotype.Service;

import com.se100.GymAndPTManagement.domain.requestDTO.ReqCreateAvailableSlotDTO;
import com.se100.GymAndPTManagement.domain.requestDTO.ReqUpdateAvailableSlotDTO;
import com.se100.GymAndPTManagement.domain.responseDTO.ResAvailableSlotDTO;
import com.se100.GymAndPTManagement.domain.responseDTO.ResultPaginationDTO;
import com.se100.GymAndPTManagement.domain.table.AvailableSlot;
import com.se100.GymAndPTManagement.domain.table.PersonalTrainer;
import com.se100.GymAndPTManagement.domain.table.Slot;
import com.se100.GymAndPTManagement.repository.AvailableSlotRepository;
import com.se100.GymAndPTManagement.repository.PersonalTrainerRepository;
import com.se100.GymAndPTManagement.repository.SlotRepository;
import com.se100.GymAndPTManagement.util.enums.DayOfWeekEnum;
import com.se100.GymAndPTManagement.util.error.IdInvalidException;

import lombok.RequiredArgsConstructor;

/**
 * Generated by: GitHub Copilot (Claude Sonnet 4.5)
 * Created by: PhDuy2005
 * Created at: 2026-01-14 10:19:59
 * Purpose: Service for AvailableSlot entity
 */
@Service
@RequiredArgsConstructor
public class AvailableSlotService {

    private final AvailableSlotRepository availableSlotRepository;
    private final PersonalTrainerRepository personalTrainerRepository;
    private final SlotRepository slotRepository;

    public ResAvailableSlotDTO createAvailableSlot(ReqCreateAvailableSlotDTO request) {
        // Validate PT exists
        PersonalTrainer pt = personalTrainerRepository.findById(request.getPtId())
                .orElseThrow(() -> new IdInvalidException("Personal trainer not found with id: " + request.getPtId()));

        // Validate Slot exists
        Slot slot = slotRepository.findById(request.getSlotId())
                .orElseThrow(() -> new IdInvalidException("Slot not found with id: " + request.getSlotId()));

        AvailableSlot availableSlot = AvailableSlot.builder()
                .personalTrainer(pt)
                .slot(slot)
                .dayOfWeek(request.getDayOfWeek())
                .isAvailable(true)
                .build();

        AvailableSlot savedAvailableSlot = availableSlotRepository.save(availableSlot);
        return ResAvailableSlotDTO.fromEntity(savedAvailableSlot);
    }

    public List<ResAvailableSlotDTO> getAllAvailableSlots() {
        return availableSlotRepository.findAll().stream()
                .map(ResAvailableSlotDTO::fromEntity)
                .collect(Collectors.toList());
    }

    public ResAvailableSlotDTO getAvailableSlotById(Long id) {
        AvailableSlot availableSlot = availableSlotRepository.findById(id)
                .orElseThrow(() -> new IdInvalidException("Available slot not found with id: " + id));
        return ResAvailableSlotDTO.fromEntity(availableSlot);
    }

    public List<ResAvailableSlotDTO> getAvailableSlotsByPtId(Long ptId) {
        // Validate PT exists
        if (!personalTrainerRepository.existsById(ptId)) {
            throw new IdInvalidException("Personal trainer not found with id: " + ptId);
        }

        return availableSlotRepository.findByPersonalTrainerId(ptId).stream()
                .map(ResAvailableSlotDTO::fromEntity)
                .collect(Collectors.toList());
    }

    public List<ResAvailableSlotDTO> getAvailableSlotsByPtIdAndAvailable(Long ptId) {
        // Validate PT exists
        if (!personalTrainerRepository.existsById(ptId)) {
            throw new IdInvalidException("Personal trainer not found with id: " + ptId);
        }

        return availableSlotRepository.findByPersonalTrainerIdAndIsAvailable(ptId, true).stream()
                .map(ResAvailableSlotDTO::fromEntity)
                .collect(Collectors.toList());
    }

    public List<ResAvailableSlotDTO> getAllAvailableSlotsByStatus(Boolean isAvailable) {
        return availableSlotRepository.findByIsAvailable(isAvailable).stream()
                .map(ResAvailableSlotDTO::fromEntity)
                .collect(Collectors.toList());
    }

    public ResAvailableSlotDTO updateAvailableSlot(Long id, ReqUpdateAvailableSlotDTO request) {
        AvailableSlot availableSlot = availableSlotRepository.findById(id)
                .orElseThrow(() -> new IdInvalidException("Available slot not found with id: " + id));

        // Validate Slot exists
        Slot slot = slotRepository.findById(request.getSlotId())
                .orElseThrow(() -> new IdInvalidException("Slot not found with id: " + request.getSlotId()));

        availableSlot.setSlot(slot);
        availableSlot.setDayOfWeek(request.getDayOfWeek());

        AvailableSlot updatedAvailableSlot = availableSlotRepository.save(availableSlot);
        return ResAvailableSlotDTO.fromEntity(updatedAvailableSlot);
    }

    public ResAvailableSlotDTO setAvailableStatus(Long id, Boolean isAvailable) {
        AvailableSlot availableSlot = availableSlotRepository.findById(id)
                .orElseThrow(() -> new IdInvalidException("Available slot not found with id: " + id));

        availableSlot.setIsAvailable(isAvailable);
        AvailableSlot updated = availableSlotRepository.save(availableSlot);
        return ResAvailableSlotDTO.fromEntity(updated);
    }

    public void deleteAvailableSlot(Long id) {
        AvailableSlot availableSlot = availableSlotRepository.findById(id)
                .orElseThrow(() -> new IdInvalidException("Available slot not found with id: " + id));
        availableSlot.setIsAvailable(false);
        availableSlotRepository.save(availableSlot);
    }

    public ResAvailableSlotDTO setAvailableBySlotPtDay(Long slotId, Long ptId, String dayOfWeekStr) {
        // Parse dayOfWeek string to enum
        DayOfWeekEnum dayOfWeek;
        try {
            dayOfWeek = DayOfWeekEnum.valueOf(dayOfWeekStr.toUpperCase());
        } catch (IllegalArgumentException e) {
            throw new IllegalArgumentException("Invalid day of week: " + dayOfWeekStr);
        }

        // Validate PT exists
        if (!personalTrainerRepository.existsById(ptId)) {
            throw new IdInvalidException("Personal trainer not found with id: " + ptId);
        }

        // Validate Slot exists
        if (!slotRepository.existsById(slotId)) {
            throw new IdInvalidException("Slot not found with id: " + slotId);
        }

        // Find available slot by slotId, ptId, dayOfWeek
        AvailableSlot availableSlot = availableSlotRepository
                .findBySlotIdAndPersonalTrainerIdAndDayOfWeek(slotId, ptId, dayOfWeek)
                .orElseThrow(() -> new IdInvalidException(
                        String.format("Available slot not found with slotId: %d, ptId: %d, dayOfWeek: %s",
                                slotId, ptId, dayOfWeekStr)));

        availableSlot.setIsAvailable(true);
        AvailableSlot updated = availableSlotRepository.save(availableSlot);
        return ResAvailableSlotDTO.fromEntity(updated);
    }

    public ResultPaginationDTO handleFetchAvailableSlots(Specification<AvailableSlot> specification,
            Pageable pageable) {
        Page<AvailableSlot> availableSlotPage = availableSlotRepository.findAll(specification, pageable);

        ResultPaginationDTO result = new ResultPaginationDTO();
        ResultPaginationDTO.Meta meta = new ResultPaginationDTO.Meta();

        meta.setPage(pageable.getPageNumber() + 1);
        meta.setPageSize(pageable.getPageSize());
        meta.setTotalPages(availableSlotPage.getTotalPages());
        meta.setTotalItems(availableSlotPage.getTotalElements());

        result.setMeta(meta);

        List<ResAvailableSlotDTO> availableSlotDTOs = availableSlotPage.getContent().stream()
                .map(ResAvailableSlotDTO::fromEntity)
                .collect(Collectors.toList());

        result.setResult(availableSlotDTOs);

        return result;
    }
}
